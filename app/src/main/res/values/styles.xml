<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/custom_brown</item>
        <item name="colorPrimaryDark">@color/custom_brown_darker</item>
        <item name="android:activatedBackgroundIndicator">@drawable/list_item_selector</item>
        <item name="android:textColorPrimary">@color/custom_blue</item>
    </style>

</resources>
